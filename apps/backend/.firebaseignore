# Deploy only the .deploy folder
# Ignore everything at root level except .deploy
/*
!/.deploy/

# Still ignore these even if they end up in .deploy somehow
.git/
.gitignore
*.log
firebase-debug*.log
firebase-debug.*.log
*.local
.DS_Store
node_modules

